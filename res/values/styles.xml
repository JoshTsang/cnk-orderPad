<?xml version="1.0" encoding="UTF-8"?>
<resources>
    <style name="TANCStyle" parent="@android:style/Theme.Dialog">
        <!-- 更换背景图片实现全透明 -->
        <item name="android:windowBackground">@drawable/background_transparent</item>
        <!-- 屏幕背景不变暗 -->
        <item name="android:backgroundDimEnabled">false</item>
        
        <item name="android:windowNoTitle">true</item> 
        <!-- 更改对话框标题栏 -->
        <!-- <item name="android:windowTitleStyle">@style/TitleStyle</item> -->
    </style>
    <style name="FULLTANCStyle">
        <!-- 更换背景图片实现全透明 -->
        <item name="android:windowBackground">@drawable/background_transparent</item>
        <!-- 屏幕背景不变暗 -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item> 
        <!-- 更改对话框标题栏 -->
        <!-- <item name="android:windowTitleStyle">@style/TitleStyle</item> -->
    </style>
    <style name="TitleText" parent="@android:style/TextAppearance.DialogWindowTitle">
        <!-- 设置Dialog标题栏文字颜色。 -->
        <item name="android:textColor">#000</item>
    </style>
    <style name="settingRow">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">15dp</item>
        <item name="android:background">@drawable/setting_table_row</item>
    </style>
    <style name="statisticInfos_1024x720">
        <item name="android:textColor">#4D2412</item>
        <item name="android:textSize">32sp</item>
    </style>
</resources>